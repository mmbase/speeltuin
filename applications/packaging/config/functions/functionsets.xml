<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE functionsets PUBLIC "//MMBase - Application//" "http://www.mmbase.org/dtd/functionsets_1_0.dtd">
<functionsets>
        <!-- this is very wrong, but since the function/thememanager/packaging -->
        <!-- are installed at the same time we do it like this. since i don't -->
        <!-- know how to merge with 'ant install' -->
        <functionset name="thememanager" file="sets/thememanager_functionset.xml" />
        <functionset name="mmpm" file="sets/mmpm_functionset.xml" />
        <functionset name="mmpb" file="sets/mmpb_functionset.xml" />
</functionsets>
